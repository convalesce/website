# Google Analytics 4 measurement id. Without it, GA is not loaded at all —
# so local dev and preview deploys stay out of your reporting.
# Add the real value in Vercel: Project → Settings → Environment Variables.
NEXT_PUBLIC_GA_ID=
